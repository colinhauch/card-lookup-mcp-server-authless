import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#683C1C',
      light: '#ae6041',
      dark: '#ae6041',
    },
    background: {
      default: '#4D88C3',
      paper: '#ffffff',
    },
  },
});
